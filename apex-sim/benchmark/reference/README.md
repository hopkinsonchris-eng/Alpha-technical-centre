# Reference results

Reference simulator outputs (tNavigator / ECLIPSE / OPM Flow) per deck, stored as
summary vectors for comparison. Keep provenance (simulator + version) alongside
each set.

```
reference/<deck>/<simulator>-<version>/summary.json
```
