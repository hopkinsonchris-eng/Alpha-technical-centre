// ===========================================================================
// apex-sim · timestep/sequential
// ---------------------------------------------------------------------------
// Sequential-implicit step: solve pressure then transport implicitly, iterating to consistency (SFI).
// ===========================================================================

export function sequentialStep(/* model, state, dt */) { /* TODO */ return { ok: false }; }
