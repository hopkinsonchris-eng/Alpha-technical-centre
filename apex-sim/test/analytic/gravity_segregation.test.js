// analytic · gravity_segregation
// Gravity segregation / equilibration column vs. hydrostatic solution.
import { strict as assert } from 'node:assert';

export async function run() {
  // TODO: run a small apex-sim case and compare against the analytic solution
  //       within tolerance. See docs/physics/.
  assert.ok(true, 'placeholder');
}
