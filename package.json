{
  "name": "alpha-technical-centre",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Alpha Technical Centre site. Static pages; this manifest exists so the calculators can be unit tested.",
  "scripts": {
    "test": "node --test \"test/*.test.mjs\""
  }
}
